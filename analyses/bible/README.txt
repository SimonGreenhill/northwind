README

0. Edit path details in lib.py for specify path to paralleltext git repository clone.

1. Run check.py to identify which bibles we can use.
	Save this output to bibles.txt

2. Run install.py to copy the bibles listed in bibles.txt into ./bibles/

3. Run make.py 
    make.py | sh